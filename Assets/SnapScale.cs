using UnityEngine;
using System.Collections;

public class SnapScale : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Debug.Log(renderer.bounds.size.x);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
